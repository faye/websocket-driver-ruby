sudo: false
language: ruby

rvm:
  - 1.9.3
  - 2.0.0
  - 2.1.10
  - 2.2.8
  - 2.3.5
  - 2.4.2
  - jruby-19mode
  - jruby-9

before_install:
  - '[[ "$(ruby --version)" == *"1.9.3"* ]] && gem update --system 2.4.8 || true'

script:
  - bundle exec rake compile
  - bundle exec rspec -c spec
